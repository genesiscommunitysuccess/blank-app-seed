<rapid-layout-region type="tabs">
    {{#each route.tiles}}
    <rapid-layout-item title="{{this.title}}" registration="{{this.title}}">
        <{{kebabCase this.componentName}}></{{kebabCase this.componentName}}>
    </rapid-layout-item>
    {{/each}}
</rapid-layout-region>
