<zero-grid-pro>
  <grid-pro-genesis-datasource
    resource-name="{{config.resourceName}}"
    {{#if config.snapshot}}
    isSnapshot="{{config.resourceName}}"
    {{/if}}
  >
  </grid-pro-genesis-datasource>
</zero-grid-pro>
