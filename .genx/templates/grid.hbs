<zero-grid-pro>
  <grid-pro-genesis-datasource
    resource-name="{{config.resourceName}}"
    {{#if config.snapshot}}
    isSnapshot="{{config.snapshot}}"
    {{/if}}
  >
  </grid-pro-genesis-datasource>
</zero-grid-pro>
