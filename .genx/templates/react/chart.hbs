<rapid-g2plot-chart
  type="{{ config.type }}"
  config={chartConfig}
>
  <chart-datasource
    resourceName="{{ config.resourceName }}"
    server-fields="{{ config.xField }} {{ config.yField }}"
  ></chart-datasource>
</rapid-g2plot-chart>
