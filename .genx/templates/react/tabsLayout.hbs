const defaultLayout = {
  global: {},
  layout: {
    type: 'row',
    children: [
      {
        type: 'tabset',
        weight: 100,
        children: [
          {{#each route.tiles}}
          { type: 'tab', name: '{{this.title}}', component: '{{camelCase this.componentName}}' },
          {{/each}}
        ],
      },
    ],
  },
};
